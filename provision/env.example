# GitHub token (needs repo, issues, checks permissions).
# Must be a fine-grained PAT (github_pat_…) or classic PAT (ghp_…).
# Do NOT use an OAuth app token (gho_…): the worker's ProtectHome=read-only
# systemd sandbox prevents gh from refreshing it, causing auth failures.
GITHUB_TOKEN=ghp_your_token_here
GH_TOKEN=ghp_your_token_here

# Claude Code OAuth token from `claude setup-token`. This is the supported
# Claude runtime credential. ANTHROPIC_API_KEY is not accepted — workers
# inject the task credential as CLAUDE_CODE_OAUTH_TOKEN.
CLAUDE_CODE_OAUTH_TOKEN=your_setup_token_here

# For additional providers (Grok, future) — credential fallback when
# providers[].credential is omitted/empty in orchestrator config.
# These are read by _parse_provider_entry (see shared/config.py).
# XAI_API_KEY=your_xai_key_here
# GROK_API_KEY=...
